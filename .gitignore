# ── OS ──
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini

# ── Editor ──
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp
*~

# ── Node (jika nanti pakai build tool) ──
node_modules/
dist/
dist-check/
build/
.cache/
*.log
npm-debug.log*
yarn-error.log*

# ── Environment & rahasia ──
# .env berisi ID analytics — jangan ikut di-commit
.env
.env.*
!.env.example
*.key
*.pem

# ── Sementara ──
tmp/
temp/
*.tmp
*.bak

# ── Sisa uji coba tooling ──
.__*
__probe.txt

# ── Rem tangan deploy otomatis ──
# Kalau berkas ini ada, tools/auto-deploy.sh berhenti bekerja.
# Sifatnya sementara & personal, jadi tidak perlu ikut ke GitHub.
.deploy-pause

# ── Berkas sementara Vite (muncul saat build, aman dihapus) ──
vite.config.js.timestamp-*
vite.config.*.timestamp-*

# ── Skrip verifikasi sementara ──
ssr-check.mjs
src/ssr-entry.jsx
legacy-cdn.html

# ── Backend Apps Script ──
# Memuat SECRET_KEY, ID Sheet/Drive, dan email pengurus.
# Repo ini publik — jangan pernah di-commit. Lihat apps-script/README.md
apps-script/Code.gs
apps-script/*.gs
